# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ProspectAI — Production Environment Template
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# ⚠️ DO NOT commit real values to .env.production!
#    Real secrets go in Portainer → Stack → Environment Variables
#
# This file is ONLY used for local Docker Compose testing.
# Copy to .env.production and fill in your values:
#   cp .env.production.example .env.production
#
# In Portainer (production), configure these vars in the stack settings UI.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

NODE_ENV=production
PORT=3000

# App URL
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Database (SQLite via bind mount in Docker)
TURSO_CONNECTION_URL=file:/data/prospectai.db

# Redis (Docker internal networking)
REDIS_URL=redis://redis:6379

# Evolution API (WhatsApp)
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=

# Apify (Google Maps + Instagram scraping)
APIFY_TOKEN=

# Better Auth (session encryption)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=https://your-domain.com

# AI Providers (at least one required)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
